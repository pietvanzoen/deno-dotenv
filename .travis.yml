language: python

install:
- [[ ! -x $HOME/.deno/bin/deno ]] && curl -L https://deno.land/x/install/install.py | python
- export PATH="$HOME/.deno/bin:$PATH"

script:
  - make test

cache:
  directories:
   - $HOME/.deno
